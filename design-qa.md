# 編隊總覽與作戰選關驗證

final result: passed

## 比較來源

- 設計：`artifacts/design/home-lobby-2026-09-08/squad-overview-revised.png`，853 × 1844，正規化至 390 × 844。
- 實作：`artifacts/validation/squad-lobby/roster-390.png`，390 × 844 CSS px，DPR 1。
- 同框比對：`artifacts/validation/squad-lobby/comparison.png`，780 × 844，左設計右實作。
- 狀態：編隊總覽、五位正式隊員、雷娜隊長、尚未編輯。實際帳號芙蕾與希雅穿夏日造型，保留玩家裝備，未為截圖改裝。

## 修正紀錄

第一輪 `roster-390-before.png`：P1，共用手機樣式把 grid 覆寫為 flex，五人肖像帶過矮、有多餘內距。
已提升總覽選擇器優先順序，指定 grid、零內距及 34% 肖像列。第二輪截圖與同框比對確認修正，底部操作、五人肖像與姓名全部在一屏內。

## 視覺檢查

- 字體：沿用遊戲既有繁中文字體與字重；姓名、隊長、角色詳情與主按鈕可讀，無截斷。標題較概念圖克制，列為 P3 可選調整。
- 版面：肖像列、狀態列、隊長大立繪、編隊按鈕及底部導覽完整；無遮擋。編輯頁只保留「確認編隊」，依最新要求移除「暫存返回」。
- 色彩：深藍底、金色選取與姓名、珊瑚色主按鈕，與既有遊戲一致。
- 圖片：採真實角色資產及玩家目前裝備；未使用生成示意人物代替。立繪裁切比概念圖稍寬，保留武器和角色辨識，屬 P3 美術微調。
- 文案：總覽沒有關卡與續玩卡片。作戰分頁選關，主流程為關卡情報 → 開始作戰。暫停頁使用「返回戰鬥」，離場明示本局結束。
- 無需另裁局部比對：完整 390 px 圖已能判斷姓名、肖像比例及控制項。

## 行為驗證

以 CUA 瀏覽器操作，後半段使用 `127.0.0.1:5174` 獨立 origin，避開使用者正在操作的 localhost 編隊。

- 總覽 → 編隊 → 修改隊長；正式小隊仍為雷娜。
- 切換作戰再返回編隊，正式小隊不變；重新編輯仍保留璃音隊長草稿。
- 確認編隊後，總覽更新為璃音隊長。
- 編輯頁「暫存返回」已移除，僅有「確認編隊」。
- 選關 → 情報 → 開始新局；防線 1000、時間從起點開始。
- 暫停 → 離開關卡：回到選關，續玩按鈕數為 0。
- 再進關從起點開始；重載頁面回到選關，無舊局恢復。
- 測試分頁 console error 為 0。
- 26 項編隊草稿／存檔測試通過；型別檢查與建置通過，16 套角色動作素材驗證通過。

## 驗證範圍

本輪驗證 390 × 844 總覽視覺與瀏覽器主流程；未重新執行整套既有 Playwright 回歸測試。舊測試中預期直接進入可修改全員列表或跨重載續玩的案例，需依新產品規則更新，不能當作本輪已通過項目。
